# crawler output + inputs
/data/

# python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# notebooks
.ipynb_checkpoints/

# local databases
*.db
*.db-wal
*.db-shm

# os
.DS_Store
/.claude/
/.vscode/

# secrets. "Several MinIO object operations.md" carries a live access/secret key pair,
# and was one `git add .` away from being published. Credentials belong in the
# environment (MINIO_ACCESS_KEY / MINIO_SECRET_KEY), never in a tracked file.
Several MinIO object operations.md
*.env
.env
.envrc
credentials.json
